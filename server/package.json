{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "npx prisma migrate deploy && node src/index.js",
    "build": "npm install && npx prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "socket.io": "^4.8.3",
    
    "_comment": "REMOVE the ^ symbol below / 以下の ^ 記号を削除してください",
    "@prisma/client": "5.10.2",
    "prisma": "5.10.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
